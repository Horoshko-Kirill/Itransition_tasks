@{
    Layout = "_Layout";
    ViewData["Title"] = "ExportToCRM";
}

<h2>Result of sending</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}

<a href="/Home/Index" class="btn btn-secondary">Back to home</a>
